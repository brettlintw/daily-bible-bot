name: KITT Bible Trigger
on:
  schedule:
    - cron: '*/5 * * * *' # 改為每 5 分鐘執行，徹底消滅時差
  workflow_dispatch:
jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger KITT
        run: curl "https://daily-bible-bot-a5smaqx...streamlit.app/?action=trigger_push&key=KITT_SECURE_KEY_2026"
