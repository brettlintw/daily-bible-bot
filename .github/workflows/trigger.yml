name: KITT Bible Trigger

on:
  # 將 cron 修改為一個永不會觸發的時間，例如 2月30日 (系統會忽略)
  schedule:
    - cron: '0 0 30 2 *' 
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "Trigger is currently disabled in favor of UptimeRobot."
